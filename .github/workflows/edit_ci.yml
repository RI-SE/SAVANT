name: Edit_CI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - dev
  workflow_call:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python and uv
        uses: astral-sh/setup-uv@v3
        with:
          python-version: '3.12'
          enable-cache: true

      - name: Sync dependencies
        working-directory: ./edit/
        run: uv sync --dev --group edit

      - name: Lint
        working-directory: ./edit
        run: |
          uv tool run flake8 .

      - name: Test
        run: uv run test-edit
